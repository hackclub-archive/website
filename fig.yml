db:
  image: orchardup/postgresql
  ports:
    - "5432"
web:
  build: .
  command: gin
  volumes:
    - .:/go/src/github.com/hackedu/backend
  ports:
    - "3000:3000"
  links:
    - db
  environment:
    SMTP_USERNAME: zachlatta
    SMTP_PASSWORD: Xk$azw0O@RVzHFIG%qa0N2wW2e
    SMTP_HOST: smtp.sendgrid.com
    SMTP_PORT: 587
